<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat" />
    <style name="TextAppearance.Material.Small" parent="android:TextAppearance.Material.Small.Inverse">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">16.dp</item>
    </style>
    <style name="TextAppearance.Material.Large" parent="android:TextAppearance.Material.Large">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">18.dp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
